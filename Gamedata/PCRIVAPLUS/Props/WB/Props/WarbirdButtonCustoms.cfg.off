// STANDARD ACTION BUTTONS---------------------------------------

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_LIGHTS_ON
	  operator = AND

	  SOURCE_VARIABLE
	  {
		name = LIGHTS
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}	
	
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_LIGHTS_OFF
	  operator = AND

	  SOURCE_VARIABLE
	  {
		name = LIGHTS
		range = -0.1, 0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}		
	
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_RCSBUTTON_ON
	  operator = AND

	  SOURCE_VARIABLE
	  {
		name = RCS
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }
	}

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_RCSBUTTON_OFF
	  operator = AND

	  SOURCE_VARIABLE
	  {
		name = RCS
		range = -0.1, 0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }
	}	
	
	
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASBUTTON_ON
	  operator = AND

	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }
	}
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASBUTTON_OFF
	  operator = AND

	  SOURCE_VARIABLE
	  {
		name = SAS
		range = -0.1, 0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }
	}	
	
// SAS MODES --------------------------------------

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASSTAB_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODESTABILITY
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}		
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASPROGRADE_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODEPROGRADE
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}	
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASRETROGRADE_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODEPROGRADE
		range = -1.1, -0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}	

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASNORMAL_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODENORMAL
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}
	
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASANTINORMAL_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODENORMAL
		range = -1.1, -0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}			

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASRADIALIN_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODERADIAL
		range = -1.1, -0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}			

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASRADIALOUT_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODERADIAL
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}			

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASTARGET_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODETARGET
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}		

RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASANTITARGET_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODETARGET
		range = -1.1, -0.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}	
	
RPM_CUSTOM_VARIABLE

	{
	  name = WARBIRD_SASMANEUVER_ON
	  operator = AND
	  
	  SOURCE_VARIABLE
	  {
		name = SAS
		range = 0.1, 1.1
	  }

	  SOURCE_VARIABLE
	  {
		name = SASMODEMANEUVER 
		range = 0.1, 1.1
	  }
	  SOURCE_VARIABLE
	  {
		name = CUSTOM_WARBIRD_ELECTRICAL_OFF
		range = -0.1, 0.1
	  }

	}		